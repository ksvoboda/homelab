apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: vpn-creds
  namespace: media
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-store
    kind: ClusterSecretStore
  target:
    name: vpn-creds
  data:
  - secretKey: wireguard_private_key
    remoteRef:
      key: 6420995f-05fd-45a9-be9d-b423010a78e8
